$(document).ready(function (){    $('#salesReportForm').submit(function (event){        event.preventDefault()        const formData = new FormData(this)        formData.append('action','CreateSaleReport')            $.ajax({                url: '../../controller/SalesController.php',                type: 'POST',                data: formData,                processData: false,                contentType: false,                success: function (data){                    $('tbody').html(data)                }            })    })    // Trigger PDF download on button click    $('#download_pdf').on('click', function() {        const startDate = $('#startDate').val();        const endDate = $('#EndDate').val();        // Ensure both startDate and endDate are provided before making the request        if (startDate && endDate) {            // Open a new window to trigger the PDF generation            window.open('../../controller/SalesController.php?startDate=' + startDate + '&endDate=' + endDate, '_blank');        } else {            Swal.fire({                text: 'Please select a date range.',                confirmButtonColor: '#4361ee',                iconColor: '#ff6347',                icon: 'warning',                toast: true,                target: 'body',                position: 'top-end',                showConfirmButton: false,                timer: 2000,  // Auto-close the alert after 2 seconds                timerProgressBar: true,  // Show the progress bar for the timer            });        }    });})