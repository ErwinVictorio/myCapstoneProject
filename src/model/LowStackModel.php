<?phpnamespace model;use config\DbConnection;include_once '../config/DbConnection.php';class LowStackModel extends DbConnection{    public function DisplayLowStack()    {        $result = $this->Connect()->query("SELECT DISTINCT product_name, current_stack, item_color,id, branch FROM product WHERE current_stack <= 5 AND status = 'Active' ORDER BY current_stack");        $dataRow = [];        if ($result->num_rows > 0){            while ($row = $result->fetch_assoc()){                $dataRow[] = $row;            }            return $dataRow;        }        return  null;    }    public function updateStack($id)    {        $Query  = "select current_stack,id from product where id = ?";        $stmt = $this->Connect()->prepare($Query);        $stmt->bind_param('i',$id);        $stmt->execute();        $result = $stmt->get_result();        $dataRow = [];        if ($result->num_rows > 0){            while ( $row = $result->fetch_assoc()){                $dataRow[] = $row;            }            return $dataRow;        }else{            return  null;        }    }    public function saveUpdatedStack($stack,$id)    {        $query = "UPDATE product SET current_stack = ? WHERE id = ?";        $stmt = $this->Connect()->prepare($query);        $stmt->bind_param('si',$stack,$id);        if ($stmt->execute()){            echo json_encode(['success' => true , 'message' => 'Successfully Updated']);        }else{            echo json_encode(['success' => false , 'error' => $stmt->error]);        }    }}