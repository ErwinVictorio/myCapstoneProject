$(document).ready(function (){   $('#form_f_creating_B').submit(function (e){       e.preventDefault()       const forData = new FormData(this)       forData.append('action','create')           $.ajax({               url: '../../controller/branchController.php',               type: 'post',               data: forData,               contentType: false,               processData: false,               dataType: 'json',               success: function (data){                   if (data.success === false){                       Swal.fire({                           text: data.message,                           confirmButtonColor: '#4361ee',                           iconColor: '#ff6347',                           icon: 'warning',                           toast: true,                           target: 'modal',                           position: 'top-end',                           showConfirmButton: false,                           timer: 2000,  // The alert will auto-close after 5 seconds                           timerProgressBar: true,  // Show the progress bar for the timer                       });                   }else {                       Swal.fire({                           text: data.message,                           icon: "success",                           confirmButtonColor: '#4361ee',                           iconColor: '#4361ee'                       });                       $('#modal_create_branch').modal('hide');                       setTimeout(() => {                           window.location.reload();                       }, 2000);                   }               }           })   })    $(document).on('click','#delete_branch',function (e){        Swal.fire({            title: "Are you sure?",            text: "You won't be able to revert this!",            icon: "warning",            showCancelButton: true,            confirmButtonColor: '#4361ee',            iconColor: '#4361ee',            confirmButtonText: "Yes, delete it!"        }).then((result) => {            if (result.isConfirmed) {             // ajax                $.ajax({                    url: '../../controller/branchController.php',                    type: 'POST',                    data: {                        id: e.target.value,                        action: 'delete'                    },                    dataType: 'json',                    success: function (data){                        if (data.success === true){                            Swal.fire({                                text: data.message,                                icon: "success",                                confirmButtonColor: '#4361ee',                                iconColor: '#4361ee'                            });                            setTimeout(() => {                                window.location.reload();                            }, 2000);                        }                    }                })            }        });    })    $(document).on('click','#update_branch',function (e){        $.ajax({            url: '../../controller/branchController.php',            type: 'POST',            data:{                action: 'update',                id: e.target.value            },            success: function (data){                // If modal doesn't exist, append; otherwise, replace its content                if ($('#modal_update_branch').length === 0) {                    $('body').append(data);                } else {                    $('#modal_update_branch').replaceWith(data);                }                // Show the modal                $('#modal_update_branch').modal('show');            }        })    })    $(document).on('submit','#form_f_update',function (e){        e.preventDefault()        const formData = new FormData(this)        formData.append('action','SaveBranch')        for (const [key, value] of formData.entries()) {            console.log(`${key}: ${value}`);        }        $.ajax({            url: '../../controller/branchController.php',            type: 'POST',            dataType: 'json',            data: formData,            contentType: false,            processData: false,            success: function (data){                if (data.success === true){                    Swal.fire({                        text: data.message,                        icon: "success",                        confirmButtonColor: '#4361ee',                        iconColor: '#4361ee'                    });                    setTimeout(() => {                        window.location.reload();                    }, 2000);                }            }        })    })})